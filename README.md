## Setup Instructions

1. Create a new folder named `engine`.
2. Download LC0 from the official website.
3. Download the Maia network weights (e.g., `1500.pb`).
4. Place `lc0.exe` and the network file inside the `engine` folder.

## Install & Run

1. Install dependencies:

   npm install

2. Start the application:

   npm start
